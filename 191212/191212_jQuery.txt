191212_jQuery


아직 dao, service가 없으니까 

mvc1 : 요청을 하면 비즈니스 로직과 출력을 한 페이지에서 이루어지도록함
mvc2 : 최초로 컨트롤러에서 요청을 받아서 
    service -> dao mapper실행 -> dao리턴 -> service 리턴 -> 컨트롤러로 리턴
    그 결과(비즈니스로직 결과)를 view 페이지로 보내서 출력


포워드 = 1. 정보공유. 실 데이터를 공유하는게 아니고, 데이터를 공유하기위해서 
	컨트롤러에서 뷰로 넘어갈때 리퀘스트, 리스폰스가 공유되는거임
	2. 페이지가 이동될때 was 서버 내에서 이동함 = 주소줄이 안바뀌는겨
리다이렉트 = 1. 한번 응답했다가 다시 요청하는거라서 리퀘스트, 리스폰스 공유 안됨
	2. 페이지 이동


~~~~ 133p
아직은 service , dao 없으니까 html, jsp만 이용해서 서버에서 가져올거임
jsp에서 smc 객체 가져오고, smc("~~") 이것도 다 jsp에서 할거임
결과를 저장해서 json 데이터로 만들건데
우리가 만들건 json obj arr 이란말여
그래서 [ {}, {}, ... {} ] 이런식으로 만들건데

[  <% 여기에 스크립틀릿으로 객체만들고 데이터가져오고 for문돌리고해  %> ]

그리고 스크립틀릿에서 json 데이터 만들어야하는데
스크립틀릿 안에는 json 데이터가 못들어간대
중간에 스크립틀릿을 끊어줘서 가야됨

[		--> json 배열 시작하는거임
<% 
~~~~
for (int i=0; i<list.size(); i++) {
  MemberVO mem = list.get(i);
  if (i>0) out.print(",");
%>				--> 스크립틀릿 끊기!
  {
    "id"   : "<%= mem.getMem_id() %>",	--> json 데이터 작성. "" 빼먹지마!
    "name" : "<%= mem.getMem_name() %>",
	...
  }
<%
}		-> for문 끊기
%>
]		--> json arr 닫기


db에서 가져올때 jsp 파일내에서 해당데이터를 
html형식으로 보낼수도있고 json으로 보낼수도 있거든?
근데 html로 보내면 형식이 고정된 데이터를 보내는거라서 가져와서 응용할수가없음
근데 json으로 가져오면 그 데이터를 응용해서 다른걸 ㅎ ㅏㄹ수가 있어
그래서 ajax로 가져올땐 주로 json으로 가져오는거임



~~~ 134p
여태했던 error 얘기중입니듀
error의 매개변수로는 result, status, error 이렇게 있다네용.. 뭐야 request도 있잖여
근데 뭐,, 별로 중요하지않은거같은딩


~~~ 137p
lprod 테이블 정보 가져오깅
 (1) vo를 만들었고요
 (2) sqlmapconfig파일에 vo파일 alias 등록합니다, xml 매퍼 등록합니다
 (3) xml 만들었읍니다








