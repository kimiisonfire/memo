190828_java

메서드는 하나의 일만 해야해, 동사적인 의미를 갖고있음

어떤 행위를 하는 메서드 add()
-> 사용자가 5,7을넣었다? => 12를 돌려줌 -->> 이런 역할을 하는 친구가 메서드

우리는 그동안 입출력없는것만 배웠다
1) 입력 o 출력 o
2) 입력 o 출력 xx
3) 입력 x 출력 x
4) 입력 x 출력 o
의 경우들이 있음

return; 으로 끝나면 반환값이 없는거라서 사실 안써도 그만이고 앞에 void 씀
void를 썼다 하면 반환하는 값이 없어야돼

메서드명, 매개변수 설정 하고 구현하고 반환값 리턴


반복문에서는 나갈때 break인것처럼 메서드는 return 만나면 밑에거 수행안하고 그자리에서 나가버림

*******************************
자바스크립트에서는 중간에 return을 만나면 지금까지했던걸 취소하면서 끝남
근데 자바에서는 중간에 return을 만나면 거기서 그냥 멈추고 다음을 실행
*******************************


클래스에 대한 설명을 할때는 꼭 독주석 /**/ 달아주십쇼

메서드는
메서드명->매개변수->수행할문장->반환하냐?->반환타입


매개변수는 메서드를 호출할때 부른 값하고 메서드의 값을 이어준다~ 해서 매개



사칙연산은 NyCalc의 메서드를 사용하면 댐다



호출할떄 주는값은 인자값
메서드가 받는값은 매개변수












