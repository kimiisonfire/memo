191015_highjava

~~ 간략소개 ~~
입출력의 기준 : 내가 지금 작성중인 프로그램이겠죠;

stream은 byte/char 단위로 데이터를 꺼내오거나 저장할수있어

바이트기반 : inputStrim WriteStream
ch14 입출력.ppt 에서 6p 는 정말이지 기본입니다,, 이정도는 알고계세요
int read(byte[] b)  -> 배열의 크기만큼 읽어오는데
크기가 20이고 읽어온게 10개밖에없으면 데이터를 다읽어서 10개만 반환함

문자기반 : Reader, Writer
문자기반이니까 문자배열 char[]가 사용됨

보조스트림은 반드시 기반스트림이 먼저 있어야댐
가장 많이쓰는게 BufferedInputStream
------------------------------------------------------
InputStream, OutputStream
스트림의 사용을 위한 권한을 얻어오고, 사용을 마치면 권한을 반납(close() )
read() 하면 첫번째 데이터를 읽어옴
mark() 해놓고 reset()하면 마크해놓은 상태로 돌아간대
근데 mark()를 지원하지 않을수도있으니까 체크하는 markSupported()

Reader, Writer
단위가 char
-------------------------------------------------------
byte 타입의 배열로 읽어올때
배열로 단순히 처리하면 오류가 발생할 가능성이 있음
길이가 안맞으면 원래 값이나 중간데 들어왔던 값으로덮어쓰임


system.in == byte 기반의 입력용 스트림 객체임


콘솔에서 입력받아 파일로 저장하는거 하는중인데
콘솔 입력을 끝내고싶을때는 ctrl+z


buffer 하고있는데 
데이터에 대한 쓰기 작업을 하는경우에는 특히 buffered는 flush()를 꼭해줘라
작업을 종료하기 전에 버퍼에 남아있는 데이터를 모두 출력하는거거든 flush()











