191209_jQuery


어떤 기능(show, hide같은)이 실행된 다음에 바로 다음동작을 넣고싶으면
$(버튼).클릭(){
   $(선택자).show(~~, function(){ 그다음에 실행할 기능 })   -- 이렇게하래

$('#result img').fadeout(1000, function() {
	$(this).attr('src', hh);
	$(this).fadein(1000);
 })


~~~ 112p
애니메이션!
"" 시간 "" 의 경과에 따라 속성값을 주기적으로 바꾸는거임
애니메이트 메소드는 값에 숫자를 사용할 수 있는 css 속성에서만 작동함
margin, padding, width, height, left, right, position, letterspacing 이런게있음
참고로 position은  relative, absolute, fixed, static  이런 속성이 있고요

$(선택자).animate ( {속성:설정값},  시간주기  )
$(“myBtn”).animate({left:”100px”} , 500)
== 버튼의 left 속성을 100px로 줄건데 500밀리세컨드동안 바꾸는거임
저 속성값은 필수로 넣어줘야됨
한꺼번에 여러개줄거면 중괄호안에 주면 되는거임


+ left, right, top, bottom 이친구들은 style의 position이 있어야 움직여..!
	relative, absolute 상관없음

+ 계속 움직이게하고싶ㅊ으면 animate를 setInterval 안에 넣어줘


~~ 114p
애니메이션명령은 큐에 저장되기때문에
첫번째로 지정한게 가장 먼저 실행됨
애니메이션을 걸고 그다음에 delay를 붙여주면 delay가 걸림

$('#lion').animate({'left':'+300px'}, 1000).delay(500) ;  이렇게!


~~ 116p
Queue = 콜백 , 백그라운드에서 펑션을 자동으로 호출하는 콜백
속성을 자유롭게 조작할수있댜
this로 받을수도 있고요
이걸 끝내고싶으면 dequeue 해서 나와야한다네용


 
animate3.html 하는중
이벤트주는 함수말고 함수를 만들어서 쓰는거 하고있음
함수를 만들어서 호출할때는 
function fx() {}
fx();  이렇게 호출하면 되는데 
저 만들었던 함수 속에서 본인을 재귀호출하려면
function fx() {
  $(~~).~~~(1000, fx)   <<-- 함수를호출하는게아니라 변수를부르는거라 괄호없이!
}

움직이고있는애를 고르고싶으면 기본필터의 :animated 이걸 쓸수있다구..!


==================================================
이제 ajax.... 미친.... 마음의준비를하래.....
==================================================

118p

서버와 데이터를 교환하는 기술 = ajax
ajax 기술을 이용해서 데이터를 주고받는다구
그 데이터는 어떤 데이터? = xml 데이터
근데 요즘은 json 데이터를 많이쓴다고 xml안하고 json 쓸거래

그리고 완전히 mvc2 패턴이고요

비동기 ==> 요청과 응답이 1:1이 아니라는거임
동기 = 한번 요청하고 그거에대한 응답을 받아야 그다음 그거를 하는거임..
비동기 = 한번 요청하면 뭐 그다음은 상관없이 내가 또 요청보낼수있는거임
카톡을봐람 내가 하나 보냈다고해서 그사람이 답장할때까지 기달려야댐? 아니쥬


웹브라우저에서 요청하면 웹서버가 처리해서 브라우저에게 새페이지를 보내줌
근데 전체페이지를 다시만드는게 아니고 필요한것만 만들어서 보내는게 ajax 쓰는거

아니,, 설명한걸 안듣고 딴짓을 하니까 모르겠네,,,
일단 123p 예제를 한대
기존 방식에서는 form태그에서 버튼누르면 다른페이지로 넘어가거나하는데
ajax 방식으로는 새로 만들지않고 기졵의 페이지에다가 일부분의 영역에 응답줄거임


